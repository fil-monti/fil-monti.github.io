---
interface Props {
  id?: string;
  class?: string;
  background?: string;
  transition?: string;
  hidden?: boolean;
}

const {
  id,
  class: className,
  background,
  transition,
  hidden = false
} = Astro.props;
---

{!hidden && (
<section
  id={id}
  class:list={["cgsi-slide", className]}
  data-background={background}
  data-transition={transition}
>
  <slot />
</section>
)}
